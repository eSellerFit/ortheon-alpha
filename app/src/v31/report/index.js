/**
 * Ortheon MVP Cut v3.1 — Report Utility Exports
 *
 * Bundle 18B: user-facing report view model builder.
 * Pure deterministic utilities — no Firestore, no AI, no API calls.
 */

export * from "./buildV31UserFacingReportViewModel.js";
